{
  "version": 2,
  "builds": [
    { "src": "index.html", "use": "@vercel/static" }
  ],
  "routes": [
    // SPA fallback — serve index.html for all frontend routes
    { "src": "^/(.*)$", "dest": "/index.html" }
  ]
}