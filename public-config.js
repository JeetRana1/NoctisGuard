// Public client-side configuration (safe to commit)
// Replace CLIENT_ID with your Discord application Client ID (public)
window.NG_CONFIG = {
  CLIENT_ID: '1463677793761230874',
  BASE_URL: window.location.origin,
  DEFAULT_PERMISSIONS: '8',
  ENABLE_INVITE_REDIRECT: false,
  // When true, login will use the mock auth (local development). Set to false in production.
  USE_MOCK: false
};