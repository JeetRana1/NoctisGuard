# .env.vercel — example env file for Vercel (do NOT commit secrets to public repos)
# NOTE: Add these as Environment Variables in the Project Settings on Vercel, do NOT commit real secrets.

# Discord OAuth / App credentials
DISCORD_CLIENT_ID=1463677793761230874
DISCORD_CLIENT_SECRET=your_discord_client_secret
# Optional aliases (some code prefers CLIENT_ID / CLIENT_SECRET)
CLIENT_ID=${DISCORD_CLIENT_ID}
CLIENT_SECRET=${DISCORD_CLIENT_SECRET}

# Public URL where the dashboard is hosted (set to your Vercel domain)
BASE_URL=https://noctis-guard.vercel.app
# Vercel automatically sets VERCEL_URL — the server code will use it as a fallback if BASE_URL is unset

# Local dev port (ignored on Vercel but handy for local testing)
PORT=3000

# Invite redirect behavior (if you enable invite redirect, ensure the redirect URI is added in Discord Developer Portal)
ENABLE_INVITE_REDIRECT=true

# Dashboard ↔ Bot authentication and endpoints
# Shared secret valid for /bot-event and bot webhook endpoints
WEBHOOK_SECRET=0e635c3099f561988459019fa1bdb07ba7945be03ec672f1669aa2d36a43d26f
# Secret the dashboard sends to the bot as x-dashboard-secret when notifying plugin updates
BOT_NOTIFY_SECRET=0e635c3099f561988459019fa1bdb07ba7945be03ec672f1669aa2d36a43d26f
# Where the dashboard will POST plugin updates (set to your bot host's webhook endpoint)
BOT_NOTIFY_URL=0e635c3099f561988459019fa1bdb07ba7945be03ec672f1669aa2d36a43d26f
# Optional: public base for bot presence endpoints
BOT_PRESENCE_URL=https://noctis-guard.vercel.app
# Optional: bot webhook listener port
BOT_WEBHOOK_PORT=4000
# Request timing / retries (optional tuning)
BOT_NOTIFY_TIMEOUT_MS=30000
BOT_NOTIFY_RETRIES=1
BOT_LIST_TIMEOUT_MS=3000
BOT_LIST_RETRIES=0
BOT_NOTIFY_REROLL_TIMEOUT_MS=60000
BOT_NOTIFY_REROLL_RETRIES=1
BOT_STATS_POLL_INTERVAL_MS=300000

# Dashboard base (where the bot can reach dashboard APIs)
DASHBOARD_BASE=${BASE_URL}

# Optional: bot token if you want the dashboard to call Discord directly (be careful)
DISCORD_BOT_TOKEN=MTQ2MzY3Nzc5Mzc2MTIzMDg3NA.GJXnv2.6y-2iaVhAZtizS-GF40iI2B_V9m2O6yScoDEB4
# Optional: if bot user id differs from client id
# DISCORD_BOT_ID=

# App display metadata
APP_NAME=NoctisGuard
APP_ICON_URL=
LEVEL_BANNER_URL=

# Notes:
# - On Vercel, add actual secret values via Project Settings > Environment Variables.
# - Do NOT commit real tokens or secrets into the repository. If exposed, rotate them immediately.

